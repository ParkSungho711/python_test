import tkinter as tk
from tkinter import messagebox
from tkinter import scrolledtext

# --- 파이썬 코딩활용능력 1급 모의고사 (실무/흐름 추적 중심 40문제 풀세트) ---
quiz_data = [
    {
        "question": "1. 다음 코드의 출력 결과는?\n\na = 'apple,banana,cherry'\nprint(len(a.split(',')))",
        "options": ["1", "2", "3", "에러 발생"],
        "answer": 2,
        "hint": "split(',')은 쉼표를 기준으로 문자열을 잘라 리스트로 만듭니다. 과일이 몇 개인지 세어보세요."
    },
    {
        "question": "2. 리스트 슬라이싱의 결과로 알맞은 것은?\n\nlst = [10, 20, 30, 40, 50]\nprint(lst[1::2])",
        "options": ["[10, 30, 50]", "[20, 40]", "[20, 30, 40]", "[10, 20]"],
        "answer": 1,
        "hint": "[시작:끝:간격] 입니다. 인덱스 1번부터 끝까지 2칸씩 건너뛰며 추출합니다."
    },
    {
        "question": "3. 딕셔너리의 get() 메서드 활용 결과는?\n\nd = {'a': 100, 'b': 200}\nprint(d.get('c', 0))",
        "options": ["100", "200", "0", "KeyError"],
        "answer": 2,
        "hint": "get(키, 기본값)은 키가 존재하지 않을 때 에러 대신 지정한 기본값을 반환합니다."
    },
    {
        "question": "4. 다음 튜플 패킹/언패킹의 결과는?\n\na, *b, c = 1, 2, 3, 4, 5\nprint(b)",
        "options": ["2", "[2, 3, 4]", "(2, 3, 4)", "에러 발생"],
        "answer": 1,
        "hint": "변수 앞에 별표(*)를 붙이면 남은 요소들을 모두 '리스트' 형태로 묶어 받습니다."
    },
    {
        "question": "5. 세트(Set) 자료형의 특징을 묻는 문제입니다. 결과는?\n\ns = {1, 2, 2, 3, 3, 3}\nprint(len(s))",
        "options": ["3", "4", "5", "6"],
        "answer": 0,
        "hint": "집합(Set)은 중복된 값을 허용하지 않습니다. 고유한 숫자가 몇 개인지 보세요."
    },
    {
        "question": "6. 다음 for문 추적의 결과는?\n\ntot = 0\nfor i in range(4):\n    if i == 2:\n        continue\n    tot += i\nprint(tot)",
        "options": ["3", "4", "5", "6"],
        "answer": 1,
        "hint": "0, 1, 2, 3 중에서 i가 2일 때는 더하지 않고 건너뜁니다. (0 + 1 + 3)"
    },
    {
        "question": "7. 이중 for문의 총 실행 횟수는?\n\ncnt = 0\nfor i in range(2):\n    for j in range(3):\n        cnt += 1\nprint(cnt)",
        "options": ["2", "3", "5", "6"],
        "answer": 3,
        "hint": "바깥 루프 2번, 안쪽 루프 3번이 곱해져 전체 반복 횟수가 결정됩니다."
    },
    {
        "question": "8. 다음 while문의 종료 후 n의 값은?\n\nn = 5\nwhile n > 0:\n    n -= 2\nprint(n)",
        "options": ["1", "0", "-1", "-2"],
        "answer": 2,
        "hint": "5 -> 3 -> 1 -> 그 다음 2를 빼면 음수가 되어 조건이 거짓(False)이 되고 루프가 끝납니다."
    },
    {
        "question": "9. if-elif 구문의 실행 결과는?\n\nx = 10\nif x > 5:\n    x = 5\nelif x == 5:\n    x = 0\nprint(x)",
        "options": ["0", "5", "10", "에러 발생"],
        "answer": 1,
        "hint": "if문이 참이면 그 안의 코드를 실행하고 전체 조건문을 빠져나옵니다. elif는 검사하지 않습니다."
    },
    {
        "question": "10. 독립된 if문 연속 사용의 결과는?\n\nx = 5\nif x > 2:\n    x += 2\nif x > 6:\n    x += 2\nprint(x)",
        "options": ["5", "7", "9", "11"],
        "answer": 2,
        "hint": "elif가 아니므로 첫 번째 if문을 거쳐 변한 x의 값을 가지고 두 번째 if문도 독립적으로 검사합니다."
    },
    {
        "question": "11. for-else 구문의 특징입니다. 결과는?\n\nfor i in range(2):\n    pass\nelse:\n    print('A')",
        "options": ["출력 없음", "A (1번 출력)", "A (2번 출력)", "에러 발생"],
        "answer": 1,
        "hint": "반복문이 break 등으로 중간에 끊기지 않고 정상적으로 끝까지 완료되면 else 블록이 한 번 실행됩니다."
    },
    {
        "question": "12. break가 포함된 for-else 구문입니다.\n\nfor i in range(3):\n    break\nelse:\n    print('A')",
        "options": ["출력 없음", "A", "에러 발생", "무한 루프"],
        "answer": 0,
        "hint": "break로 인해 반복문이 강제 종료되었으므로 else 블록은 보상을 주지 않고 건너뜁니다."
    },
    {
        "question": "13. 문자열 누적(반전) 코드의 결과는?\n\ns = ''\nfor c in 'abc':\n    s = c + s\nprint(s)",
        "options": ["abc", "bca", "cab", "cba"],
        "answer": 3,
        "hint": "기존 문자열 s의 '앞'에 새로운 문자 c를 계속 붙입니다. 'a' -> 'ba' -> 'cba'"
    },
    {
        "question": "14. range 함수의 감소(step) 활용 결과는?\n\nprint(sum(range(5, 0, -2)))",
        "options": ["5", "8", "9", "15"],
        "answer": 2,
        "hint": "5부터 0 직전까지 2씩 감소하는 숫자는 5, 3, 1 입니다. 이를 모두 더합니다."
    },
    {
        "question": "15. 무한 루프와 break의 조합입니다.\n\nn = 0\nwhile True:\n    n += 1\n    if n == 3:\n        break\nprint(n)",
        "options": ["1", "2", "3", "4"],
        "answer": 2,
        "hint": "n이 1, 2, 3이 되는 순간 break를 만나 while문이 즉시 종료됩니다."
    },
    {
        "question": "16. 키워드 인자(Keyword Argument) 전달 결과는?\n\ndef f(a, b):\n    return a - b\nprint(f(b=2, a=5))",
        "options": ["-3", "3", "7", "에러 발생"],
        "answer": 1,
        "hint": "매개변수 이름을 명시하여 인자를 전달하면 순서가 바뀌어도 올바르게 매핑됩니다. (5 - 2)"
    },
    {
        "question": "17. 전역 변수를 함수 내에서 수정하는 방법은?\n\nx = 1\ndef f():\n    global x\n    x += 1\nf()\nprint(x)",
        "options": ["1", "2", "3", "에러 발생"],
        "answer": 1,
        "hint": "global 키워드를 사용하면 함수 안에서 바깥에 있는 x를 직접 수정할 수 있습니다."
    },
    {
        "question": "18. 파이썬에서 함수가 여러 값을 반환할 때의 특징은?\n\ndef f():\n    return 1, 2\na, b = f()\nprint(a)",
        "options": ["1", "2", "(1, 2)", "에러 발생"],
        "answer": 0,
        "hint": "쉼표로 여러 값을 반환하면 하나의 '튜플'로 묶여 반환되며, 이를 다시 언패킹(Unpacking)하여 받습니다."
    },
    {
        "question": "19. 함수에 가변 객체(리스트)를 전달했을 때의 결과는?\n\ndef f(lst):\n    lst[0] = 9\na = [1, 2]\nf(a)\nprint(a[0])",
        "options": ["1", "2", "9", "에러 발생"],
        "answer": 2,
        "hint": "리스트는 메모리 주소를 참조하므로, 함수 내부에서 리스트 요소를 수정하면 원본 리스트도 함께 변경됩니다."
    },
    {
        "question": "20. 내장 함수 max()의 동작 결과는?\n\nprint(max([1, 5, 3, 4]))",
        "options": ["1", "3", "4", "5"],
        "answer": 3,
        "hint": "리스트 내의 요소 중 가장 큰(maximum) 값을 반환합니다."
    },
    {
        "question": "21. 내장 함수 sum()의 두 번째 인자 활용 결과는?\n\nprint(sum([1, 2], 10))",
        "options": ["3", "10", "13", "에러 발생"],
        "answer": 2,
        "hint": "sum(iterable, start)는 시작값(10)에 리스트의 요소들(1, 2)을 누적하여 더합니다."
    },
    {
        "question": "22. 숫자의 절대값을 구하는 내장 함수는 무엇인가요?",
        "options": ["absolute()", "abs()", "math.abs()", "val()"],
        "answer": 1,
        "hint": "absolute의 앞 세 글자를 사용합니다."
    },
    {
        "question": "23. 변수의 자료형을 확인하는 코드의 결과는?\n\nprint(type(1.0) == float)",
        "options": ["True", "False", "1", "에러 발생"],
        "answer": 0,
        "hint": "1.0은 소수점이 있는 실수(float) 자료형입니다."
    },
    {
        "question": "24. 예외 처리(try-except)의 흐름 추적입니다.\n\ntry:\n    res = 1 / 0\nexcept ZeroDivisionError:\n    res = 1\nprint(res)",
        "options": ["0", "1", "ZeroDivisionError", "None"],
        "answer": 1,
        "hint": "0으로 나누는 순간 에러가 발생하여 except 블록으로 넘어가 res 변수가 1로 할당됩니다."
    },
    {
        "question": "25. 에러 발생 여부와 무관하게 무조건 실행되는 finally 블록의 결과는?\n\ntry:\n    pass\nfinally:\n    print(2)",
        "options": ["출력 없음", "pass", "2", "에러 발생"],
        "answer": 2,
        "hint": "try 블록이 정상 실행되든 에러가 나든 finally 블록의 코드는 항상 마지막에 실행됩니다."
    },
    {
        "question": "26. 멤버십 연산자 in의 활용 결과는?\n\nprint('a' in 'abc')",
        "options": ["True", "False", "1", "0"],
        "answer": 0,
        "hint": "문자열 'abc' 안에 'a'라는 문자가 포함되어 있는지 묻고 있습니다."
    },
    {
        "question": "27. 논리값(bool) 평가의 결과는?\n\nprint(bool(\"\"))",
        "options": ["True", "False", "None", "에러 발생"],
        "answer": 1,
        "hint": "파이썬에서 길이가 0인 빈 문자열은 논리적으로 거짓(False)으로 평가됩니다."
    },
    {
        "question": "28. 리스트의 요소를 확장하는 메서드의 결과는?\n\nlst = [1]\nlst.extend([2, 3])\nprint(lst)",
        "options": ["[1, [2, 3]]", "[[2, 3], 1]", "[1, 2, 3]", "에러 발생"],
        "answer": 2,
        "hint": "append는 리스트 자체를 하나로 넣지만, extend는 리스트 안의 요소들을 풀어서 이어 붙입니다."
    },
    {
        "question": "29. 딕셔너리 병합 및 업데이트 메서드의 결과는?\n\nd = {'a': 1}\nd.update({'b': 2})\nprint(len(d))",
        "options": ["1", "2", "3", "에러 발생"],
        "answer": 1,
        "hint": "기존 딕셔너리에 새로운 딕셔너리의 키-값 쌍을 합칩니다."
    },
    {
        "question": "30. 리스트에서 특정 값의 위치를 찾는 메서드는?\n\nlst = [10, 20, 30]\nprint(lst.index(20))",
        "options": ["0", "1", "2", "20"],
        "answer": 1,
        "hint": "파이썬의 인덱스는 0부터 시작합니다. 20은 두 번째에 위치합니다."
    },
    {
        "question": "31. 리스트의 문자열 요소들을 하나로 결합하는 결과는?\n\nprint(\"-\".join([\"a\", \"b\", \"c\"]))",
        "options": ["abc-", "-abc", "a-b-c", "에러 발생"],
        "answer": 2,
        "hint": "join 메서드를 호출하는 문자열('-')이 리스트의 각 요소 사이에 삽입됩니다."
    },
    {
        "question": "32. 문자 빈도수 구하기 알고리즘 패턴의 결과는?\n\nd = {}\nfor c in \"aba\":\n    d[c] = d.get(c, 0) + 1\nprint(d['a'])",
        "options": ["1", "2", "3", "KeyError"],
        "answer": 1,
        "hint": "'aba' 문자열에서 'a' 문자가 총 몇 번 등장하는지 세는 코드입니다."
    },
    {
        "question": "33. 논리 연산자(and, or)의 평가 결과는?\n\na = 3\nb = 5\nprint(a > 2 and b < 10)",
        "options": ["True", "False", "1", "0"],
        "answer": 0,
        "hint": "a > 2 도 참(True)이고, b < 10 도 참(True)입니다. 둘 다 참이면 and 연산의 결과는 참입니다."
    },
    {
        "question": "34. 두 변수의 값을 서로 교환(Swap)하는 결과는?\n\na = 1\nb = 2\na, b = b, a\nprint(a)",
        "options": ["1", "2", "3", "에러 발생"],
        "answer": 1,
        "hint": "파이썬에서는 임시 변수 없이 튜플 언패킹을 이용해 두 변수의 값을 한 줄로 우아하게 교환할 수 있습니다."
    },
    {
        "question": "35. 나눗셈의 '몫'만 구하는 연산자의 결과는?\n\nprint(7 // 2)",
        "options": ["3.5", "3", "1", "4"],
        "answer": 1,
        "hint": "// 연산자는 소수점 이하를 버리고 정수 몫만 반환합니다."
    },
    {
        "question": "36. 나눗셈의 '나머지'만 구하는 연산자의 결과는?\n\nprint(7 % 2)",
        "options": ["3.5", "3", "1", "0"],
        "answer": 2,
        "hint": "7을 2로 나누면 몫은 3이고 나머지가 얼마 남는지 생각해 보세요."
    },
    {
        "question": "37. 거듭제곱 연산자의 결과는?\n\nprint(2 ** 3)",
        "options": ["5", "6", "8", "9"],
        "answer": 2,
        "hint": "** 기호는 앞의 숫자를 뒤의 숫자만큼 곱합니다. (2를 3번 곱함)"
    },
    {
        "question": "38. 리스트에서 마지막 요소를 꺼내면서 삭제하는 결과는?\n\nlst = [1, 2, 3]\nval = lst.pop()\nprint(val)",
        "options": ["1", "2", "3", "None"],
        "answer": 2,
        "hint": "pop() 메서드에 인덱스를 주지 않으면 기본적으로 맨 뒤(마지막) 요소를 뽑아냅니다."
    },
    {
        "question": "39. 리스트 내에 특정 값이 몇 개 있는지 세는 결과는?\n\nlst = [1, 2, 2, 3]\nprint(lst.count(2))",
        "options": ["1", "2", "3", "4"],
        "answer": 1,
        "hint": "리스트 안에 숫자 2가 총 몇 개 들어있는지 묻는 메서드입니다."
    },
    {
        "question": "40. 문자열의 일부 문자를 다른 문자로 바꾸는 결과는?\n\ns = \"apple\"\nprint(s.replace(\"p\", \"b\"))",
        "options": ["abble", "apble", "abple", "bpple"],
        "answer": 0,
        "hint": "replace 메서드는 조건에 맞는 모든 문자를 찾아 일괄적으로 교체합니다."
    }
]

class QuizApp:
    def __init__(self, root):
        self.root = root
        self.root.title("파이썬 코딩활용능력 1급 (40문제 풀세트)")
        self.root.geometry("850x650")
        self.root.configure(bg="#F0F9FF") # 청량한 라이트 블루 배경
        
        self.score = 0
        self.current_question_idx = 0
        self.wrong_answers = [] 
        
        # 타이틀 바 (딥 오션 블루)
        self.title_frame = tk.Frame(root, bg="#0284C7", pady=15)
        self.title_frame.pack(fill="x")
        self.title_label = tk.Label(self.title_frame, text="파이썬 실전 코드 추적 40제 완벽 마스터", font=("맑은 고딕", 16, "bold"), fg="white", bg="#0284C7")
        self.title_label.pack()

        # 중앙 카드
        self.card_frame = tk.Frame(root, bg="white", padx=30, pady=30, highlightbackground="#BAE6FD", highlightthickness=2)
        self.card_frame.pack(fill="both", expand=True, padx=40, pady=30)
        
        self.progress_label = tk.Label(self.card_frame, text="", font=("맑은 고딕", 11, "bold"), fg="#64748B", bg="white")
        self.progress_label.pack(anchor="e", pady=(0, 15))

        self.question_label = tk.Label(self.card_frame, text="", font=("Consolas", 12, "bold"), fg="#0F172A", bg="white", justify="left", wraplength=700)
        self.question_label.pack(anchor="w", pady=(0, 20))
        
        self.options_frame = tk.Frame(self.card_frame, bg="white")
        self.options_frame.pack(fill="x", pady=10)
        
        self.var = tk.IntVar()
        self.radio_buttons = []
        for i in range(4):
            rb = tk.Radiobutton(self.options_frame, text="", variable=self.var, value=i, 
                                font=("맑은 고딕", 11), fg="#334155", bg="white",
                                activebackground="white", selectcolor="white",
                                justify="left", wraplength=650)
            rb.pack(anchor="w", pady=6)
            self.radio_buttons.append(rb)
            
        self.button_frame = tk.Frame(self.card_frame, bg="white")
        self.button_frame.pack(fill="x", pady=(20, 0))

        self.hint_visible = False
        self.hint_button = tk.Button(self.button_frame, text="💡 힌트 보기", command=self.toggle_hint, 
                                     font=("맑은 고딕", 10, "bold"), bg="#FEF08A", fg="#854D0E", 
                                     activebackground="#FDE047", relief="flat", cursor="hand2", padx=10, pady=5)
        self.hint_button.pack(side="left")
        
        self.next_button = tk.Button(self.button_frame, text="다음 문제 ➔", command=self.next_question, 
                                     font=("맑은 고딕", 11, "bold"), bg="#0284C7", fg="white", 
                                     activebackground="#0369A1", relief="flat", cursor="hand2", padx=15, pady=5)
        self.next_button.pack(side="right")
        
        self.hint_label = tk.Label(self.card_frame, text="", font=("맑은 고딕", 10, "italic"), fg="#0369A1", bg="#E0F2FE", 
                                   justify="left", wraplength=700, padx=10, pady=10)

        self.load_question()

    def toggle_hint(self):
        if self.hint_visible:
            self.hint_label.pack_forget()
            self.hint_button.config(text="💡 힌트 보기")
            self.hint_visible = False
        else:
            self.hint_label.pack(before=self.button_frame, fill="x", pady=(0, 15))
            self.hint_button.config(text="💡 힌트 닫기")
            self.hint_visible = True

    def load_question(self):
        self.var.set(-1) 
        if self.hint_visible:
            self.toggle_hint()
            
        q_data = quiz_data[self.current_question_idx]
        self.progress_label.config(text=f"진행 상황: {self.current_question_idx + 1} / {len(quiz_data)}")
        self.question_label.config(text=q_data["question"])
        self.hint_label.config(text=f"힌트: {q_data['hint']}")
        
        for i in range(4):
            self.radio_buttons[i].config(text=q_data["options"][i])
            
        if self.current_question_idx == len(quiz_data) - 1:
            self.next_button.config(text="최종 결과 확인 ✔️", bg="#E11D48", activebackground="#BE123C")

    def next_question(self):
        selected_option = self.var.get()
        if selected_option == -1:
            messagebox.showwarning("안내", "답을 하나 선택해 주세요!")
            return
            
        q_data = quiz_data[self.current_question_idx]
        correct_answer = q_data["answer"]
        
        if selected_option == correct_answer:
            self.score += 1
        else:
            self.wrong_answers.append({
                "question": q_data["question"],
                "my_answer": q_data["options"][selected_option],
                "correct_answer": q_data["options"][correct_answer]
            })
            
        self.current_question_idx += 1
        if self.current_question_idx < len(quiz_data):
            self.load_question()
        else:
            self.show_result()

    def show_result(self):
        self.title_frame.destroy()
        self.card_frame.destroy()
        
        result_card = tk.Frame(self.root, bg="white", padx=30, pady=30, highlightbackground="#BAE6FD", highlightthickness=2)
        result_card.pack(fill="both", expand=True, padx=40, pady=40)
        
        score_text = f"🎉 40문제 대장정을 마쳤습니다!\n\n총 {len(quiz_data)}문제 중 {self.score}문제를 맞췄습니다."
        score_label = tk.Label(result_card, text=score_text, font=("맑은 고딕", 16, "bold"), fg="#0284C7", bg="white")
        score_label.pack(pady=(0, 20))
        
        if self.wrong_answers:
            wrong_label = tk.Label(result_card, text="[ 오답 노트 ]", font=("맑은 고딕", 12, "bold"), bg="white", fg="#475569")
            wrong_label.pack(anchor="w", pady=(0, 10))
            
            st = scrolledtext.ScrolledText(result_card, wrap=tk.WORD, font=("Consolas", 11), bg="#F8FAFC", fg="#0F172A", padx=10, pady=10)
            st.pack(fill="both", expand=True)
            
            for idx, wrong in enumerate(self.wrong_answers, 1):
                st.insert(tk.END, f"❌ 틀린 문제 {idx}:\n{wrong['question']}\n\n")
                st.insert(tk.END, f"내가 고른 답 : {wrong['my_answer']}\n")
                st.insert(tk.END, f"정답         : {wrong['correct_answer']}\n")
                st.insert(tk.END, "="*60 + "\n\n")
                
            st.config(state="disabled")
        else:
            perfect_label = tk.Label(result_card, text="💯 40문제 올클리어! 파이썬 기초 마스터입니다.", font=("맑은 고딕", 14, "bold"), fg="#059669", bg="white")
            perfect_label.pack(pady=40)

        exit_button = tk.Button(result_card, text="프로그램 종료", command=self.root.destroy, 
                                font=("맑은 고딕", 12, "bold"), bg="#64748B", fg="white", 
                                activebackground="#475569", relief="flat", cursor="hand2", padx=20, pady=10)
        exit_button.pack(pady=(20, 0))

if __name__ == "__main__":
    root = tk.Tk()
    app = QuizApp(root)
    root.mainloop()
